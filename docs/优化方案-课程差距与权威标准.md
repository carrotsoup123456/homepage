# 主页优化方案：课程要求差距 + 权威标准 + 优先清单

> 用途：优化动工前的"体检报告"。先回答"还差什么"，再用权威标准回答"往哪儿优化最值"。
> 参考源：本课程 Syllabus（Project-Based CST&AI Foundations, Fall 2026）+ Google web.dev Core Web Vitals + W3C WCAG 2.2 + NN/g 十大可用性启发式。

---

## 一、一句话结论

课程的"个人主页"部分你已经**拿满了 Phase 1–4 的主体分**（能跑、已上线、双主题、有动态、有文档）。
但现在还有 **3 个课程级硬缺口**（直接扣分），外加一批**工程级小缺口**（不影响及格，影响"成品感"）。
本文件把两者合并成一张**按优先级排序的清单**，从上往下做即可。

---

## 二、对照 Syllabus：还缺什么

### 2.1 课程原文要求（Vibe Coding 20%）

| 课程要求（原文） | 权重 | 你的状态 |
|---|---|---|
| 个人主页 Phase 1–4 | 10% | ✅ 已完成（V3 已上线） |
| **主页最终扩展**：连接 Digital Twin / 知识库，**或**实现一个重要新功能 | **5%** | ❌ **未完成** |
| 独立小项目（与主页无关的网站/小应用） | 5% | ❌ 未开始（属另一份作业） |

Vibe Coding 的四条核心主题（原文 Core Topics）：

| 核心主题 | 你的状态 |
|---|---|
| AI 辅助：从想法到能跑的原型 | ✅ 有 |
| 内容/结构/设计/功能的迭代改进 | ✅ 有（V1→V2→V3） |
| **发布可运行项目并接入合适的支持服务** | ✅ 有（GitHub Pages + FormSubmit） |
| **收集反馈、评估结果、做"有依据的改进"** | ❌ **只有表单，没有反馈闭环** |

必需过程证据（原文 Required Process Evidence）：

| 必需证据 | 你的状态 |
|---|---|
| AI 协作记录 + 关键决策记录 | ✅ 有（AI协作日志.md） |
| 重大迭代 / 版本变更 / 改进的证据 | ✅ 有（V1/V2/V3 + git 历史） |
| **可运行或可访问的原型 + 基本测试证据 + 反馈证据** | ⚠️ 原型 ✅，**测试证据弱、反馈证据无** |

### 2.2 三个课程级硬缺口

**缺口 1：5% 的"最终扩展"完全没做，而且是现成机会**
- 课程原话：requires either connecting a **Digital Twin or knowledge base**, or implementing **one significant new feature**.
- 你的 `知识库` 页面**目前是占位页**：`src/views/KnowledgeView.vue` 顶部有醒目横幅
  > `⏳ 建设中 — 当前为示例占位内容，后续将陆续导入我的真实学习笔记与项目复盘。`
- 数据源 `src/data/site.js` 里只有 **3 条示例笔记**（`note-1` Vue 组合式 API / `note-2` 监督学习 / `note-3` 项目制学习）。
- 结论：**这是最省力的 5%——把占位知识库做成"真知识库"即可。**

**缺口 2：没有"反馈收集 → 基于反馈改进"的闭环**
- 表单只是"能发消息"，属于"接入支持服务"；但课程明确要求 **Collecting feedback, evaluating results, and making evidence-based improvements**。
- 你缺的是：**真实反馈数据** + **"我根据反馈改了 X"的证据**。

**缺口 3："基本测试"证据偏弱**
- 课程要求 prototype with **basic testing** evidence。
- 现状：`package.json` 只有 `dev / build / preview`，**没有任何测试或 lint 配置**（无 Vitest / Playwright / ESLint）。
- 目前只有人工验收记录，缺"可重复运行的测试"。

> 另：`独立小项目（5%）`也还没开始。它不属于主页，但同属 Vibe Coding 20%，建议排进日程。

### 2.3 工程级小缺口（不影响及格，但一眼就能看出"没打磨完"）

| # | 缺口 | 证据 |
|---|---|---|
| 1 | **所有页面共用同一个浏览器标题** | 全站 `document.title` 仅 `刘博康 – 个人主页`，切到"关于/知识库/联系"标题不变 |
| 2 | **没有社交分享 / SEO 元信息** | `index.html` 只有 `charset` / `viewport` / `description` / `title`；无 `og:` / `twitter:` / `canonical` |
| 3 | **没有搜索引擎文件** | 无 `robots.txt`、无 `sitemap.xml` |
| 4 | **键盘可达性缺失** | 全站 `:focus-visible` 样式 **0 处**、无"跳到主内容"链接、导航无 `aria-current` |
| 5 | **没有 404 页面** | 未知网址被静默重定向回首页（`/:pathMatch(.*)*` → `/`），用户不知道自己点错了 |
| 6 | **没有跟随系统深浅色** | 主题只认 `localStorage`，默认永远 light，忽略系统设置 |
| 7 | **图片未声明尺寸** | `<img>` 均无 `width`/`height`，首屏加载有布局跳动（CLS）风险 |
| 8 | **README 还是脚手架原文** | 仍是 `# Vue 3 + Vite / This template should help get you started...` |
| 9 | **两处死文件** | `src/assets/vite.svg`（无引用，不进 dist）、`server/feedback-server.mjs`（无引用的 Plan B） |

---

## 三、网上查到的"好网站"权威标准

> 只取三家公认权威，都能直接对照到你的代码。

### 3.1 Google **Core Web Vitals**（核心网页指标）— 衡量"快不快、稳不稳"
来源：`https://web.dev/articles/vitals`、`https://web.dev/articles/defining-core-web-vitals-thresholds`

三个指标，判定按真实用户第 **75 百分位**：

| 指标 | 含义 | 好 | 需改进 | 差 |
|---|---|---|---|---|
| **LCP** Largest Contentful Paint | 主内容多久出现（加载感） | ≤ 2.5 s | 2.5–4.0 s | > 4.0 s |
| **INP** Interaction to Next Paint | 点一下多久有反应（流畅感） | ≤ 200 ms | 200–500 ms | > 500 ms |
| **CLS** Cumulative Layout Shift | 内容乱跳程度（稳定感） | ≤ 0.1 | 0.1–0.25 | > 0.25 |

**对你的指向**：CLS 是唯一有真实风险的一项——图片没写 `width/height`；LCP 受首屏大图（森林/头像）影响。

### 3.2 W3C **WCAG 2.2**（Web 内容可访问性指南）— 全世界公认的"合格线"
来源：`https://www.w3.org/WAI/standards-guidelines/wcag/`、`.../wcag/glance/`（已是 ISO/IEC 40500 国际标准）

四大原则 **POUR**：**可感知** Perceivable / **可操作** Operable / **可理解** Understandable / **健壮** Robust。

与你直接相关的条目：

| 条目 | 要求 | 你的状态 |
|---|---|---|
| 2.4.1 Bypass Blocks (A) | 提供"跳到主内容" | ❌ 无 |
| 2.4.7 Focus Visible (AA) | 键盘焦点必须看得见 | ❌ 无 `:focus-visible` |
| 2.4.2 Page Titled (A) | 每页要有自己的标题 | ❌ 全站同标题 |
| 1.1.1 Non-text Content (A) | 图片要有替代文字 | ✅ 7 张图都有 `alt` |
| 2.3.3 Animation from Interactions (AAA) | 动画可关闭 | ✅ 已做 `prefers-reduced-motion` |
| 1.4.3 Contrast (AA) | 正文对比度 ≥ 4.5:1 | ⚠️ 待实测（浅色主题的绿/石板蓝在小字上） |

### 3.3 NN/g **十大可用性启发式**（1994 提出，至今是行业事实标准）
来源：`https://www.nngroup.com/articles/ten-usability-heuristics/`

| # | 原则 | 一句话 | 你的状态 |
|---|---|---|---|
| 1 | 系统状态可见 | 让用户随时知道"我在哪、刚做了什么" | ⚠️ 导航无"当前页"标记、切页无标题变化 |
| 2 | 贴合真实世界 | 用用户的语言，不用内部黑话 | ✅ 做得好 |
| 3 | 用户控制与自由 | 随时能退出/撤销 | ✅ 有返回顶部、详情页可关闭 |
| 4 | 一致性与标准 | 同类东西长得一样、位置一样 | ✅ 做得好 |
| 5 | 错误预防 | 最好别让错误发生 | ⚠️ 表单可加校验 |
| 6 | 识别优于回忆 | 选项摆在眼前，别让人背 | ✅ 导航常驻 |
| 7 | 灵活与高效 | 给熟手快捷方式 | ⚠️ 缺键盘快捷操作 |
| 8 | 审美与极简 | 别放无关信息 | ✅ 做得好 |
| 9 | 帮用户识别/诊断/恢复错误 | 说人话 + 给出路 | ❌ 无 404 页 |
| 10 | 帮助与文档 | 需要时能查到帮助 | ⚠️ README 未写 |

---

## 四、优先优化清单（从上往下做）

排序依据 = **对最终成品/评分的提升 ÷ 工作量**。

### 🥇 第一优先：直接换分（课程硬要求）

| 序号 | 做什么 | 依据 | 改哪里 | 工作量 |
|---|---|---|---|---|
| **A1** | **把"知识库"从占位做成真知识库**：导入真实笔记（Markdown），加全文搜索、按标签/时间筛选 | 课程 5% 最终扩展明确选项 | `KnowledgeView.vue` + `site.js` + 新增 `src/content/notes/*.md` | 中 |
| **A2** | **建立反馈闭环**：加"这条对你有用吗 👍/留言"入口 → 收 5–8 条真实反馈 → 写《用户反馈与改进记录》并真的改一处 | Vibe Coding 核心主题第 4 条 + 原型评审 | 新增 `FeedbackWidget.vue` + 反馈文档 | 中 |
| **A3** | **补"基本测试"证据**：装 Vitest，写 5–8 个最小测试（路由可用、主题切换、表单校验、知识库筛选、Markdown 渲染） | 必需过程证据"basic testing" | `package.json` + `src/**/*.spec.js` | 中 |

### 🥈 第二优先：权威标准，最提"成品感"

| 序号 | 做什么 | 依据 | 改哪里 | 工作量 |
|---|---|---|---|---|
| **B1** | **每页独立标题**：`首页/关于/知识库/联系` 各自设置 `document.title`（含人名，利于分享与检索） | WCAG 2.4.2(A) + NN/g #1 | 新增 `router.afterEach` 或 `usePageTitle` | 小 |
| **B2** | **SEO / 分享元信息**：`og:title/description/image`、`twitter:card`、`canonical`，并加 `robots.txt` + `sitemap.xml` | 搜索与社交分享基础 | `index.html` + `public/` | 小 |
| **B3** | **键盘可达 + 焦点可见**：加"跳到主内容"链接、全局 `:focus-visible` 描边、导航加 `aria-current="page"` | WCAG 2.4.1 / 2.4.7 + NN/g #1 | `App.vue` + `SiteHeader.vue` + `style.css` | 小 |
| **B4** | **加 404 页面**：未知网址给一张有品牌感的"迷路了 → 回首页"页，而不是静默跳转 | NN/g #9 | 新增 `NotFoundView.vue` + 改 router | 小 |
| **B5** | **性能体检 + CLS 治理**：给所有图加 `width/height`，跑 Lighthouse 记录 LCP/CLS/INP，目标 LCP ≤2.5s、CLS ≤0.1 | Core Web Vitals | 各 view 的 `<img>` + 产出体检报告 | 小 |

### 🥉 第三优先：整洁与体验加分

| 序号 | 做什么 | 依据 | 工作量 |
|---|---|---|---|
| **C1** | 深浅色跟随系统 `prefers-color-scheme` + 首访无闪烁 | 平台惯例 | 小 |
| **C2** | 重写 `README.md`（项目简介、技术栈、本地运行、部署、目录结构） | NN/g #10 | 小 |
| **C3** | 清理死文件（`src/assets/vite.svg`、决定 `server/feedback-server.mjs` 去留） | 代码整洁 | 极小 |
| **C4** | 对比度实测并修正（浅色主题的绿/石板蓝用于小字时须 ≥4.5:1） | WCAG 1.4.3(AA) | 小 |
| **C5** | 移动端菜单/主题按钮补 `aria-expanded`、`aria-pressed` | WCAG 4.1.2 | 极小 |

---

## 五、建议的执行批次

- **第 1 批（换分）**：A1 真知识库 → A2 反馈闭环 → A3 测试。
- **第 2 批（专业度）**：B1 标题 → B2 SEO/分享 → B3 键盘可达 → B4 404 → B5 性能体检。
- **第 3 批（收尾）**：C1–C5。
- 每批做完 → 重新 build → 推送 → 线上复验 → 更新《测试与验收记录》与《AI协作日志》。
