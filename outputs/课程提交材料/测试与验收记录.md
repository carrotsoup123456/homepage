# 测试与验收记录 · 个人主页

> 本文件记录个人主页原型（V2 / V3）的功能自测、线上验收与端到端验证证据，对应课程的「基础测试与反馈证据」要求。

- **被测版本**：V2（多功能主页）/ **V3（视觉重设计，当前线上）**
- **线上地址**：https://carrotsoup123456.github.io/homepage/
- **测试环境**：macOS；Chrome（DeepWorks 内置浏览器，CDP 自动化）；Node.js；线上为 GitHub Pages
- **测试日期**：2026-09-05（本地） / 2026-09-10（线上 V2 + V3） / 2026-09-22（V3 扩展与项目库扩充）

---

## 一、功能自测（本地）

| # | 功能 | 测试步骤 | 预期 | 结果 |
|---|------|----------|------|------|
| 1 | 路由多页 | 点击导航切换 `/about`、`/knowledge`、`/contact`、`/project/carbon-brain` | 各页渲染正常 | ✅ |
| 2 | 深色模式 | 点击主题切换按钮 | `<html data-theme="dark">`，配色变化并持久化 | ✅ |
| 3 | 返回顶部 | 滚动 > 400px | 按钮出现，点击平滑回顶 | ✅ |
| 4 | 滚动渐入 | 滚动页面 | 元素进入视口淡入上移 | ✅ |
| 5 | 项目详情 | 首页点项目卡片 | 跳转 `/project/:id`，Markdown 正文 + 亮点渲染 | ✅ |
| 6 | 知识库 | 点击第 2 条笔记 | 右侧正文标题联动更新 | ✅ |
| 7 | 联系表单（本地） | 填写并提交 | 显示「已收到」，清空表单 | ✅ |
| 8 | 响应式 | 缩窄窗口 | 导航折叠、网格单列 | ✅ |
| 9 | 图片灯箱 | 点击详情页画廊图片 | 弹出全屏大图 | ✅ |

---

## 二、线上验收（GitHub Pages）

### 2.1 首页与资源

```
线上首页 → HTTP 200
h1 文本 = "刘博康"
图片 = 8 张，断图 = 0
JS bundle: /homepage/assets/index-gyERFV-P.js → HTTP 200
favicon.svg → HTTP 200
```

### 2.2 全路由巡航（CDP 实测，无缓存）

| 路由 | 结果 |
|------|------|
| `#/`（首页） | ✅ 正常渲染 |
| `#/about` | ✅ 正常渲染 |
| `#/knowledge` | ✅ 正常渲染 |
| `#/contact` | ✅ 正常渲染 |
| `#/project/stock-quant` | ✅ 正常渲染 |

> 新增 `.nojekyll` 修复了首次部署时 assets 404 的问题。

### 2.3 线上截图证据

见 `images/` 目录：
- `V2-线上-首页.png`
- `V2-线上-联系表单成功.png`

---

## 三、联系表单端到端验证（重点）

### 3.1 接口连通性（curl）

```bash
# 带浏览器来源头的请求
POST https://formsubmit.co/ajax/carrotsoup@qq.com
  带 Origin/Referer + JSON body
→ {"success":"true","message":"The form was submitted successfully."}
```

- ✅ 激活后接口返回 `success:true`。
- ℹ️ 未带 `Origin`/`Referer` 时返回「请通过 Web 服务器打开页面」——确认服务要求来自真实网页环境。

### 3.2 浏览器端到端（CDP 自动化，线上真实页面）

| 步骤 | 操作 | 观察结果 |
|------|------|----------|
| 1 | 强制整页重载 `?t=xxx#/contact`（绕过缓存） | 加载新 bundle `index-gyERFV-P.js` |
| 2 | 填表：姓名 / 邮箱 / 留言 | 输入生效（Vue v-model 同步） |
| 3 | 点击「提交」 | 触发 fetch |
| 4 | 等待响应 | 页面显示 **「✅ 已收到你的留言！我会尽快回复。」** |

**结论**：表单在**线上真实环境**端到端可用，留言真实发送到邮箱（已人工确认收信）。✅

---

## 四、调试过程中发现并修复的问题

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | 线上加载源码 `index.html`、页面空白 | 部署源为 main 根（源码）而非构建产物 | 改用 gh-pages 分支托管 dist |
| 2 | assets 首次 404 | Jekyll 处理静态资源 | 新增 `.nojekyll` 触发正确部署 |
| 3 | Formspree 邮箱验证死循环 | Formspree 侧缺陷 | 改用 FormSubmit |
| 4 | Web3Forms 不可用 | 该网络下返回 403 | 放弃，改选 FormSubmit |
| 5 | 端到端首次「Form not found」 | hash 路由跳转不重载文档，跑了旧缓存 bundle | 带时间戳整页重载后测试 |
| 6 | git push 失败（代理拒绝连接） | 代理端口 7890 未监听 | 实测直连 GitHub 可用，移除代理配置 |

---

## 五、V3 视觉重设计验收（2026-09-10）

V3 在保留 V2 全部功能的基础上重建视觉语言（画廊 / 森林夜双主题）。验收重点为**双主题渲染、素材加载、子页面一致性**。

### 5.1 本地验收（CDP 自动化截图）

| # | 项目 | 预期 | 结果 |
|---|------|------|------|
| 1 | 首页整页 · 浅色 | 全部区块布局正确、图片零缺失 | ✅ |
| 2 | 首页整页 · 深色 | 配色正确、对比度合格 | ✅ |
| 3 | 首屏 Hero（浅/深） | 森林背景 + 视差 + 画框人像 + 悬浮标签 | ✅ |
| 4 | 项目页 / 关于页 / 知识库 / 联系页 | 浅色 + 深色均正常 | ✅ |
| 5 | 深色主题对比度修复 | 主色填充文字可读、头像描边自适应 | ✅ |
| 6 | 构建 | `npm run build` 通过 | ✅ CSS 26.00 kB / JS 160.95 kB |

### 5.2 线上验收（V3 发布后）

```
线上首页 → HTTP 200
线上 bundle = /homepage/assets/index-CbvcaDLJ.js（V3+动态）+ index-BVN-yQS9.css → HTTP 200
原创素材 hero-forest.jpg(331.8KB) / moss-macro.jpg(182.0KB) → HTTP 200
JS bundle 保留 FormSubmit 端点 → formsubmit.co/ajax/carrotsoup@qq.com
CDP 线上复验：萤火虫 22 个（animationName=firefly）、.hero-title=heroEnter、
              .hero-glow 存在、滚动后统计终值 2/10/3/2026 → 与本地一致 ✅
```

> 增量发布：`gh-pages` `e610747`（5 项动态效果），线上 bundle 已更新为上述哈希。

**线上复验（2026-09-22，V3 扩展上线后 `0654db3`）**

```
远端 refs/heads/gh-pages = 0654db3  ✅
线上首页 HTTP 200，引用 assets/index-CWuxDgjj.js + index-DSUAn-FG.css（已切换）✅
新 bundle HTTP 200（167.96 kB）；旧 bundle index-CbvcaDLJ.js 返回 404 ✅
线上 bundle 内容核对：6 篇笔记标题 6/6、反馈文案齐全、homepage-feedback 记忆键、FormSubmit 端点 ✅
CDP 线上实测：#/knowledge 显示 6 篇笔记 + 10 个标签；搜索 LightGBM 6 → 1 → 6 ✅
CDP 线上实测：打开笔记滚到正文末尾，反馈组件三态正常（截图 images/V3-线上-反馈组件.png）✅
```

> **上线延迟记录**：推送后约 100 秒内线上仍指向旧 bundle，属 GitHub Pages 发布与 CDN 缓存（`max-age=600`）延迟，非部署失败；等待后复查即已切换。

### 5.3 截图证据

`images/` 中 `V3-*` 系列：
- `V3-首页首屏-浅色.png`、`V3-首页整页-深色.png`
- `V3-项目页.png`、`V3-关于页.png`、`V3-知识库-深色.png`
- `V3-线上-首页.png`

### 5.4 V3 相关问题与修复

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | 浅色整页截图画面平铺重复 | `captureBeyondViewport` 与 `will-change`/`sticky` 的 Chrome 合成 bug | 冻结 vh 高度 + 重建渲染目标后截图 |
| 2 | CDP 渲染进程卡死、截图超时 | 旧渲染进程被反复整页截图拖垮 | 关闭旧目标、经扩展新建标签页 |
| 3 | 深色主题主色填充文字对比度低 | 深色主色偏亮 + 硬编码白字 | 新增 `--color-on-primary` 令牌 |
| 4 | DeepWorks 生图带右下角水印 | 生图服务默认加水印 | 裁剪后导出 JPEG |
| 5 | 页面级调试 WebSocket 恒返回 500 | 该 Electron/Chrome 构建不支持 page 级 ws | 改用浏览器级 ws + `Target.attachToTarget({flatten:true})` + `sessionId` |
| 6 | 后台 / 遮挡标签页截图挂起（帧不推进） | 只有可见标签页才产生动画帧 | 经扩展 `open_url` 打开并激活标签页后再截图 |
| 7 | 整页截图 Hero 被 `100vh` 撑满形成假象 | `min-height:100vh` | 截图前把 `.hero-v3` 的 `minHeight` 钉为视口高 |

### 5.5 动态效果验收（5 项，2026-09-10 增量）

在 V3 视觉之上新增 5 项动态（首屏进场编排 / 森林萤火虫 / 鼠标跟随柔光 / 统计数字滚动增长 / 区块交错滑入），纯 CSS + 原生 JS，无新增依赖。

| # | 项目 | 预期 | 结果 |
|---|------|------|------|
| 1 | 首屏进场编排 | `.hero-title` 应用 `heroEnter`、`.scroll-cue` 应用 `cueEnter`，按序延迟进场 | ✅ |
| 2 | 森林萤火虫 | Hero 内 22 个 span、`animationName: firefly`、画面可见绿色光点 | ✅ |
| 3 | 鼠标跟随柔光 | `mousemove` 后 `--mx/--my` 更新、`.hero-glow` opacity=1 | ✅ `--mx=70.18% --my=42.98%` |
| 4 | 统计数字滚动增长 | 滚动进入后终值为 `2 / 10 / 3 / 2026` | ✅ |
| 5 | 区块交错滑入 | timeline 5 项 / feature-list 6 项 / stat-grid 4 项均带 `.reveal` 且按序延迟 | ✅ |
| 6 | `prefers-reduced-motion` 降级 | 萤火虫/柔光 `display:none`、进场动画 `none`、数字立即终值 | ✅ |
| 7 | 构建 | `npm run build` 通过 | ✅ CSS 27.63 kB / JS 163.01 kB |

截图证据：`outputs/V3-动态效果验证/`（9 张）。

> 完整设计过程见 [`交付部分/md文件/04-V3视觉重设计.md`](../../交付部分/md文件/04-V3视觉重设计.md)。

---

### 5.6 V3 扩展验收 —— 真实知识库 + 反馈闭环 + 自动化测试（2026-09-22）

对应课程「主页最终扩展（5%）」与要求中的 *basic testing* / *feedback evidence*。

**① 自动化测试（新增）**

接入 Vitest + @vue/test-utils + jsdom（仅 `devDependencies`，不进构建产物）。

```
$ npm test

 RUN  v5.0.1
 Test Files  5 passed (5)
      Tests  31 passed (31)
```

| 测试文件 | 覆盖内容 | 断言数 |
|----------|----------|--------|
| `tests/notes.spec.js` | 内容管线：加载、frontmatter 解析、日期倒序、标签去重、关键词+标签组合筛选、空结果 | 9 |
| `tests/site.spec.js` | 站点数据：项目 id 唯一且无空格、正文非空、详情图有 `alt`、外链为 https、含邮箱联系方式 | 5 |
| `tests/router.spec.js` | 路由表：五页路由存在、`/project/:id` 参数透传、路径可解析、未知地址有兜底 | 4 |
| `tests/feedback.spec.js` | 反馈出口：端点、`_captcha:false`、成功/失败/HTTP 非 2xx/非 JSON 四种判定 | 6 |
| `tests/feedback-widget.spec.js` | 反馈组件：三步状态流转、提交字段、本机去重、失败与异常不假装成功 | 7 |

**② 浏览器实测（浅色 + 深色）**

| # | 项目 | 预期 | 结果 |
|---|------|------|------|
| 1 | 知识库列表 | 显示 6 篇真实笔记 + 10 个标签，副标题「共 6 篇」 | ✅ |
| 2 | 知识库关键词搜索 | 输入 `LightGBM` 后列表由 6 条收敛为 1 条；清空后恢复 6 条 | ✅ 6 → 1 → 6 |
| 3 | 笔记深链 | 打开 `#/knowledge?note=carbon-brain-dac` 直接呈现该篇 | ✅ |
| 4 | 反馈组件 · 询问态 | 笔记/项目/首页末尾均出现「这部分内容对你有用吗？」 | ✅ 三处均验证 |
| 5 | 反馈组件 · 补充态 | 点「👍 有用」后展开文本框与「提交反馈 / 跳过」 | ✅ |
| 6 | 反馈组件 · 完成态 | 提交后显示「✅ 谢谢！你的反馈已收到（有用）」 | ✅ 用伪造 `fetch` 验证，未发送真实邮件 |
| 7 | 反馈组件 · 失败态 | 服务端返回 `success:false` 时显示原因，**不显示感谢** | ✅ |
| 8 | 联系表单回归 | 抽取公共出口后行为不变（端点/约定字段/成功判定一致） | ✅ |
| 9 | 深色主题下反馈组件 | `#/project/stock-quant` 切深色，反馈卡片样式正常 | ✅ |
| 10 | 构建 | `npm run build` 通过 | ✅ CSS 32.77 kB / JS 181.95 kB |

> 体积变化：CSS 27.63 → 32.77 kB，JS 163.01 → 181.95 kB。增量主要来自 6 篇 Markdown 正文被内联进包（内容即产品，属预期增长）。

**③ 截图证据**

`images/` 新增：`V3-知识库-搜索筛选.png`、`V3-知识库-深色.png`（已更新为真实内容版）、
`V3-反馈-1询问.png`、`V3-反馈-2补充说明.png`、`V3-反馈-3已提交.png`、
`V3-首页-反馈入口.png`、`V3-项目页深色-反馈.png`

**④ 本轮问题与修复**

| # | 问题 | 根因 | 处理 |
|---|------|------|------|
| 1 | 脚本截图连续 4 次超时且截到旧图，误以为知识库白屏 | 后台标签页不产生动画帧，`Page.captureScreenshot` 一直等不到帧 | 查 DOM 确认内容存在（`#app` 内 8745 字符）→ 经扩展 `open_url` 激活标签页后再截图 |
| 2 | 反馈组件截图停在上一轮状态（找不到「有用」按钮） | 组件状态未复位 | 截图前先 `Page.reload` 复位；演示用伪造 `fetch` 避免真实发信 |
| 3 | 站点数据测试断言失败 | 误以为 `site` 有 `role` 字段，实际为 `tagline` | 按真实字段修正测试（**测试纠正了对代码的错误记忆**） |

> 完整实现过程见 [`交付部分/md文件/06-V3扩展-知识库与反馈闭环.md`](../../交付部分/md文件/06-V3扩展-知识库与反馈闭环.md)。

---

### 5.7 项目库扩充与「关于」页改造验收（2026-09-22）

本批次把主页项目从 **2 个扩到 6 个**（含一个可双击运行的完整模拟游戏），「关于」页从 2 段扩到 6 段，知识库从 6 篇扩到 **10 篇**。

**① 自动化测试（断言数 31 → 36）**

```
$ npm test

 RUN  v5.0.1
 Test Files  5 passed (5)
      Tests  36 passed (36)
```

新增 5 条断言（`tests/site.spec.js`）：

| 断言 | 检查内容 | 为什么加它 |
|------|----------|-----------|
| 项目字段契约完整 | 每个项目必须有 `icon/short/desc/tech/role/highlights/images`，且 `highlights` 非空 | 防止新加项目时漏字段导致详情页渲染报错 |
| 二次开发类必须注明上游 | `todo-panel` 与 `claude-code-custom` 文案须同时命中 `/上游\|开源\|第三方\|MIT/` 与 `/不主张\|非原创\|不是我原创\|不是原创/` | **防止文案被无意改回成夸大表述** |
| 技能详情与首页跑马灯一一对应 | `skillDetails.length === skills.length` 且名称可匹配 | 防止展开面板与标签脱节 |
| 本地图片路径带 base 前缀 | 每个 `img.src` 以 `/` 开头且为图片后缀 | GitHub Pages 子路径下漏前缀会 404（V2 已踩过） |
| **项目的图片文件真的存在** | 用 `fs.existsSync(path.resolve('public', img.src))` 逐个核对 | 防止「改了图片格式却忘改引用路径」——本批次真的踩到了（见 ④-2） |

**①-b 反向验证（确认这条新断言不是"永远通过"）**

新加断言必须证明它挑剔得动，否则等于没写：

```
把 claude-code 的引用临时改回 .png →
  × 项目的图片文件真的存在
    AssertionError: claude-code-custom: /projects/claude-code/skills.png 找不到对应文件
还原为 .jpg → 36 passed
```

**② 浏览器实测（本地 dev 5174）**

| # | 项目 | 预期 | 结果 |
|---|------|------|------|
| 1 | 「关于」页区块完整性 | 4 个技能按钮 / 6 条项目索引 / 4 张原则卡 / 3 张「现在在做什么」/ 2 条教育经历 | ✅ |
| 2 | 技能展开（新交互） | 点击第一个技能标签后 `aria-expanded="true"`，面板展开显示对应内容 | ✅ |
| 3 | 无字面 Markdown 残留 | 页面上不出现裸露的 `**` | ✅ 全部区块检查通过 |
| 4 | 项目详情页渲染 | `#/project/weiguan-yifang` 标题正确、正文标题层级正确、无字面 `**` | ✅ 正文 632 字符 |
| 5 | 新素材加载 | 新引入的项目图能正常加载 | ✅ `naturalWidth = 1500` |
| 6 | 知识库篇数 | 侧边栏显示「共 10 篇」，新增 4 篇可打开 | ✅ |
| 7 | 构建 | `npm run build` 通过 | ✅ CSS 37.52 kB / JS 218.83 kB |

**③ 内容质量核查（本批次新增的一道检查）**

新增了一条**文案正确性检查**：把 Markdown 逐段解析成 HTML，扫描是否残留未被解析的 `**`。原因是在写《回测数字的陷阱》时发现 —— **`**` 后若紧跟中文标点，CommonMark 不会当作加粗闭合**，页面上就会露出裸露的 `**`。检查后修掉了 1 处。

| 检查 | 范围 | 结果 |
|------|------|------|
| Markdown 加粗解析 | 10 篇笔记 | ✅ 修正 1 处后全部通过 |
| Markdown 加粗解析 | 6 段项目正文 | ✅ 全部通过（用正则抽取模板字符串后单独解析，不依赖打包环境） |
| 图片体积 | `public/projects/` | ✅ 单张 4.5 MB → 压缩后总计 3.0 MB |

**④ 本轮问题与修复**

| # | 问题 | 根因 | 处理 |
|---|------|------|------|
| 1 | 构建直接失败（Parse failure） | 项目文案里用反引号写了 `app.py`，破坏模板字符串 | 改成普通文字表述 |
| 2 | 图片转换后引用失效 | 批量把 `.png` 转成 `.jpg` 并删除原文件，但代码里仍引用 `.png` | 把代码里的引用路径一并改掉（**改文件和改引用必须成对做**） |
| 3 | 想用 Node 直接 import `site.js` 检查文案失败 | 纯 Node 环境没有 `import.meta.env`（`asset()` 依赖它） | 改成用正则抽取模板字符串后单独解析 |
| 4 | 「关于」页技能标签"看着能点但点不动" | 原来是 `<span>` + hover 动画，没有行为 | 改成 `<button>` + `aria-expanded` 展开面板 |
| 5 | 用 CDP 检查「关于」页时命中了旧标签页 | 调试脚本按 `.find()` 取**第一个**匹配标签，多标签时命中的是旧页面 | 改为取**最后一个**匹配标签（`.filter().pop()`） |
| 6 | 截图里页面内容为空（游戏试玩页） | 未聚焦/后台标签页不产生动画帧，CSS 入场动画不推进，元素停留在 `opacity:0` | 截图前注入脚本强制 `animation/transition/opacity` 复位后再截 |

**⑤ 截图证据**

`images/` 新增：`V3-关于页-技能展开.png`、`V3-首页-项目列表.png`、`V3-项目页-为官一方.png`

> 完整实现过程见 [`交付部分/md文件/07-项目库扩充与关于页改造.md`](../../交付部分/md文件/07-项目库扩充与关于页改造.md)。

### 5.8 上线与线上复验（2026-09-22）

**发布记录**

| 分支 | 提交 | 说明 |
|------|------|------|
| `gh-pages` | `0654db3` → `fd3569e` → `c17f89c` | 站点产物；`c17f89c` 为含水印修复的最终版 |
| `main` | `7b0079d` → `e39d118` | 源码；以 `-s ours` 合并了远端原有的 V1/V2/V3 历史，**保留远端提交记录、推送为快进而非强推** |

> 推送过程中网络多次抖动（`github.com` 间歇不可达、`Failed to connect ... after 75004 ms`、`Empty reply from server`），最终以 HTTP/1.1 + 降低 `lowSpeedLimit` 的重试方式推送成功。

**线上复验（公网真实页面，CDP 实测）**

| # | 检查项 | 结果 |
|---|--------|------|
| 1 | 线上 bundle 已更新 | ✅ 页面引用 `assets/index-ELjfodgb.js` / `index-CrfiSzA_.css` |
| 2 | 首页统计显示「6 项目实践」 | ✅ |
| 3 | 首页项目入口 | ✅ 6 个：`weiguan-yifang` / `carbon-brain` / `stock-quant` / `todo-panel` / `claude-code-custom` / `ccswitch` |
| 4 | 首页图片（含经历区旧图） | ✅ 14 张全部加载，`naturalWidth` 分别为 1280/824/1500/1706/1280/1500/1500/1200/1200/900/900/750/1200/900 |
| 5 | 「关于」页技能展开 | ✅ 4 个 `button[aria-expanded]`，点击后 `false → true`，面板显示真实内容（如「Python / 主力语言 / 从数据处理到模型训练……」） |
| 6 | 「关于」页区块 | ✅ 「现在在做什么」「专业技能」「项目」「工作方式」「教育」全部存在 |
| 7 | 知识库篇数 | ✅ 显示「共 10 篇」，标签新增「工程 / 合规 / 游戏 / 复盘」 |
| 8 | 6 个项目详情页路由 | ✅ 均可打开，正文与「上游/开源/二次开发/第三方」声明描述符符合预期 |
| 9 | 全站图片 HTTP 探测 | ✅ `public/` 下 22 张图全部 `200`（其中 1 张首次探测 `000`，复测 `200`，判定为网络抖动而非 404） |

> 说明：CDP 在页面内读 `img.naturalWidth` 时，懒加载与并行加载会让少数图短暂读到 `0`；因此**判定"是否断图"以 HTTP 状态码与 `naturalWidth` 复核为准**，不能只看一次快照。

**④-b 素材水印问题（本批次发现的合规瑕疵）**

项目背景图 `public/projects/weiguan-yifang/bg.jpg` 是用文生图工具生成的，图面右下角带服务商水印「DeepWorks AI」。这是**第三方标识出现在自己的作品里**，无论是版权观感还是作品完成度都不该留。

处理过程：

| 步骤 | 做法 | 结果 |
|------|------|------|
| 1. 定位 | 用 PIL 逐像素扫描，测出水印在 1500×2250 原图中的范围：`x 1097–1446`、`y 2146–2201` | ✅ 精确到像素 |
| 2. 试探裁剪 | 先裁到 1500×1980 | ⚠️ 发现 `y2146–2250` 一带存在深色墨迹（小船），裁掉会切到画面主体，**放弃裁剪** |
| 3. 改为修补 | 取水印矩形上下各 3 行干净像素，逐列线性插值填补，再叠加 ±2 的轻微噪点打破条带感 | ✅ 尺寸保持 1500×2250 不变，画面完整 |
| 4. 验收 | 水印区亮白像素 0；边界剖面无台阶；修复区灰度标准差 5.03，落在周边干净水面自然区间 1.53–8.63 内 | ✅ 肉眼无可见色块 |
| 5. 线上确认 | 线上 `bg.jpg` 与本地 `sha256` 完全一致（`8a593921…8fed`，507,617 字节） | ✅ 线上已生效 |

---

## 六、测试结论

- **功能完整性**：V2 九大功能全部通过自测；V3 在保留全部功能的基础上通过双主题视觉验收。 ✅
- **自动化测试**：知识库内容管线、站点数据、路由表、反馈出口、反馈组件、项目字段契约与内容口径共 **36 条断言全部通过**。 ✅
- **内容准确性**：新增的 4 个工作区项目均来自真实产出（有产出才写，空目录工作区不写）；涉及数字的表述已补全口径；二次开发项目已标注上游来源。 ✅
- **素材合规**：AI 生成素材上残留的服务商水印已定位并修复（未裁剪掉画面内容），线上文件与本地 `sha256` 一致。 ✅
- **线上可用性**：公网首页与全路由渲染正常，资源加载无误；V3 原创素材与 bundle 均 200。 ✅
- **关键业务流程**：联系表单在线上真实端到端可用；内容反馈组件已在首页、项目页、笔记页三处可用。 ✅
- **满足评审标准**：项目**既可本地运行，也可公网访问**（runnable AND publicly accessible），且带有**基础测试**与**反馈机制**。 ✅

---

## 七、后续测试计划

- [ ] 补充移动端真机测试（不同机型/浏览器）。
- [ ] **收集真实用户反馈（不少于 5 条）**并填入《用户反馈与改进记录》，形成优先级修订清单。—— 课程禁止编造，须由本人实际收集
- [ ] 对知识库、项目详情等内容更新后做回归测试（`npm test` + 页面复验）。
- [ ] 按权威标准体检补齐：每页独立标题（WCAG 2.4.2）、键盘焦点与「跳到主内容」（WCAG 2.4.7 / 2.4.1）、404 页、`<img>` 补 `width/height` 治理 CLS。
- [ ] 重写 `README.md`（现仍为脚手架原文）。
- [x] ~~线上复验本批次变更（6 个项目页、「关于」页技能展开、10 篇笔记）~~ —— 已于 2026-09-22 完成，见 5.8
- [ ] 数字人对话：先做路线决策（关键约束：**API Key 不能放在前端**），见 `docs/数字人对话-准备清单.md`。
- [ ] 若后续继续用文生图素材，**交付前必须逐张检查右下角水印**（本批次踩过一次）。


---

_本记录基于真实测试过程整理，作为过程证据归档。_
