# 测试与验收记录 · 个人主页

> 本文件记录个人主页原型（V2 / V3）的功能自测、线上验收与端到端验证证据，对应课程的「基础测试与反馈证据」要求。

- **被测版本**：V2（多功能主页）/ **V3（视觉重设计，当前线上）**
- **线上地址**：https://carrotsoup123456.github.io/homepage/
- **测试环境**：macOS；Chrome（DeepWorks 内置浏览器，CDP 自动化）；Node.js；线上为 GitHub Pages
- **测试日期**：2026-09-05（本地） / 2026-09-10（线上 V2 + V3） / 2026-09-22（V3 扩展与项目库扩充）

---

## 一、功能自测（本地）

| # | 功能 | 测试步骤 | 预期 | 结果 |
|---|------|----------|------|------|
| 1 | 路由多页 | 点击导航切换 `/about`、`/knowledge`、`/contact`、`/project/carbon-brain` | 各页渲染正常 | ✅ |
| 2 | 深色模式 | 点击主题切换按钮 | `<html data-theme="dark">`，配色变化并持久化 | ✅ |
| 3 | 返回顶部 | 滚动 > 400px | 按钮出现，点击平滑回顶 | ✅ |
| 4 | 滚动渐入 | 滚动页面 | 元素进入视口淡入上移 | ✅ |
| 5 | 项目详情 | 首页点项目卡片 | 跳转 `/project/:id`，Markdown 正文 + 亮点渲染 | ✅ |
| 6 | 知识库 | 点击第 2 条笔记 | 右侧正文标题联动更新 | ✅ |
| 7 | 联系表单（本地） | 填写并提交 | 显示「已收到」，清空表单 | ✅ |
| 8 | 响应式 | 缩窄窗口 | 导航折叠、网格单列 | ✅ |
| 9 | 图片灯箱 | 点击详情页画廊图片 | 弹出全屏大图 | ✅ |

---

## 二、线上验收（GitHub Pages）

### 2.1 首页与资源

```
线上首页 → HTTP 200
h1 文本 = "刘博康"
图片 = 8 张，断图 = 0
JS bundle: /homepage/assets/index-gyERFV-P.js → HTTP 200
favicon.svg → HTTP 200
```

### 2.2 全路由巡航（CDP 实测，无缓存）

| 路由 | 结果 |
|------|------|
| `#/`（首页） | ✅ 正常渲染 |
| `#/about` | ✅ 正常渲染 |
| `#/knowledge` | ✅ 正常渲染 |
| `#/contact` | ✅ 正常渲染 |
| `#/project/stock-quant` | ✅ 正常渲染 |

> 新增 `.nojekyll` 修复了首次部署时 assets 404 的问题。

### 2.3 线上截图证据

见 `images/` 目录：
- `V2-线上-首页.png`
- `V2-线上-联系表单成功.png`

---

## 三、联系表单端到端验证（重点）

### 3.1 接口连通性（curl）

```bash
# 带浏览器来源头的请求
POST https://formsubmit.co/ajax/carrotsoup@qq.com
  带 Origin/Referer + JSON body
→ {"success":"true","message":"The form was submitted successfully."}
```

- ✅ 激活后接口返回 `success:true`。
- ℹ️ 未带 `Origin`/`Referer` 时返回「请通过 Web 服务器打开页面」——确认服务要求来自真实网页环境。

### 3.2 浏览器端到端（CDP 自动化，线上真实页面）

| 步骤 | 操作 | 观察结果 |
|------|------|----------|
| 1 | 强制整页重载 `?t=xxx#/contact`（绕过缓存） | 加载新 bundle `index-gyERFV-P.js` |
| 2 | 填表：姓名 / 邮箱 / 留言 | 输入生效（Vue v-model 同步） |
| 3 | 点击「提交」 | 触发 fetch |
| 4 | 等待响应 | 页面显示 **「✅ 已收到你的留言！我会尽快回复。」** |

**结论**：表单在**线上真实环境**端到端可用，留言真实发送到邮箱（已人工确认收信）。✅

---

## 四、调试过程中发现并修复的问题

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | 线上加载源码 `index.html`、页面空白 | 部署源为 main 根（源码）而非构建产物 | 改用 gh-pages 分支托管 dist |
| 2 | assets 首次 404 | Jekyll 处理静态资源 | 新增 `.nojekyll` 触发正确部署 |
| 3 | Formspree 邮箱验证死循环 | Formspree 侧缺陷 | 改用 FormSubmit |
| 4 | Web3Forms 不可用 | 该网络下返回 403 | 放弃，改选 FormSubmit |
| 5 | 端到端首次「Form not found」 | hash 路由跳转不重载文档，跑了旧缓存 bundle | 带时间戳整页重载后测试 |
| 6 | git push 失败（代理拒绝连接） | 代理端口 7890 未监听 | 实测直连 GitHub 可用，移除代理配置 |

---

## 五、V3 视觉重设计验收（2026-09-10）

V3 在保留 V2 全部功能的基础上重建视觉语言（画廊 / 森林夜双主题）。验收重点为**双主题渲染、素材加载、子页面一致性**。

### 5.1 本地验收（CDP 自动化截图）

| # | 项目 | 预期 | 结果 |
|---|------|------|------|
| 1 | 首页整页 · 浅色 | 全部区块布局正确、图片零缺失 | ✅ |
| 2 | 首页整页 · 深色 | 配色正确、对比度合格 | ✅ |
| 3 | 首屏 Hero（浅/深） | 森林背景 + 视差 + 画框人像 + 悬浮标签 | ✅ |
| 4 | 项目页 / 关于页 / 知识库 / 联系页 | 浅色 + 深色均正常 | ✅ |
| 5 | 深色主题对比度修复 | 主色填充文字可读、头像描边自适应 | ✅ |
| 6 | 构建 | `npm run build` 通过 | ✅ CSS 26.00 kB / JS 160.95 kB |

### 5.2 线上验收（V3 发布后）

```
线上首页 → HTTP 200
线上 bundle = /homepage/assets/index-CbvcaDLJ.js（V3+动态）+ index-BVN-yQS9.css → HTTP 200
原创素材 hero-forest.jpg(331.8KB) / moss-macro.jpg(182.0KB) → HTTP 200
JS bundle 保留 FormSubmit 端点 → formsubmit.co/ajax/carrotsoup@qq.com
CDP 线上复验：萤火虫 22 个（animationName=firefly）、.hero-title=heroEnter、
              .hero-glow 存在、滚动后统计终值 2/10/3/2026 → 与本地一致 ✅
```

> 增量发布：`gh-pages` `e610747`（5 项动态效果），线上 bundle 已更新为上述哈希。

**线上复验（2026-09-22，V3 扩展上线后 `0654db3`）**

```
远端 refs/heads/gh-pages = 0654db3  ✅
线上首页 HTTP 200，引用 assets/index-CWuxDgjj.js + index-DSUAn-FG.css（已切换）✅
新 bundle HTTP 200（167.96 kB）；旧 bundle index-CbvcaDLJ.js 返回 404 ✅
线上 bundle 内容核对：6 篇笔记标题 6/6、反馈文案齐全、homepage-feedback 记忆键、FormSubmit 端点 ✅
CDP 线上实测：#/knowledge 显示 6 篇笔记 + 10 个标签；搜索 LightGBM 6 → 1 → 6 ✅
CDP 线上实测：打开笔记滚到正文末尾，反馈组件三态正常（截图 images/V3-线上-反馈组件.png）✅
```

> **上线延迟记录**：推送后约 100 秒内线上仍指向旧 bundle，属 GitHub Pages 发布与 CDN 缓存（`max-age=600`）延迟，非部署失败；等待后复查即已切换。

### 5.3 截图证据

`images/` 中 `V3-*` 系列：
- `V3-首页首屏-浅色.png`、`V3-首页整页-深色.png`
- `V3-项目页.png`、`V3-关于页.png`、`V3-知识库-深色.png`
- `V3-线上-首页.png`

### 5.4 V3 相关问题与修复

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | 浅色整页截图画面平铺重复 | `captureBeyondViewport` 与 `will-change`/`sticky` 的 Chrome 合成 bug | 冻结 vh 高度 + 重建渲染目标后截图 |
| 2 | CDP 渲染进程卡死、截图超时 | 旧渲染进程被反复整页截图拖垮 | 关闭旧目标、经扩展新建标签页 |
| 3 | 深色主题主色填充文字对比度低 | 深色主色偏亮 + 硬编码白字 | 新增 `--color-on-primary` 令牌 |
| 4 | DeepWorks 生图带右下角水印 | 生图服务默认加水印 | 裁剪后导出 JPEG |
| 5 | 页面级调试 WebSocket 恒返回 500 | 该 Electron/Chrome 构建不支持 page 级 ws | 改用浏览器级 ws + `Target.attachToTarget({flatten:true})` + `sessionId` |
| 6 | 后台 / 遮挡标签页截图挂起（帧不推进） | 只有可见标签页才产生动画帧 | 经扩展 `open_url` 打开并激活标签页后再截图 |
| 7 | 整页截图 Hero 被 `100vh` 撑满形成假象 | `min-height:100vh` | 截图前把 `.hero-v3` 的 `minHeight` 钉为视口高 |

### 5.5 动态效果验收（5 项，2026-09-10 增量）

在 V3 视觉之上新增 5 项动态（首屏进场编排 / 森林萤火虫 / 鼠标跟随柔光 / 统计数字滚动增长 / 区块交错滑入），纯 CSS + 原生 JS，无新增依赖。

| # | 项目 | 预期 | 结果 |
|---|------|------|------|
| 1 | 首屏进场编排 | `.hero-title` 应用 `heroEnter`、`.scroll-cue` 应用 `cueEnter`，按序延迟进场 | ✅ |
| 2 | 森林萤火虫 | Hero 内 22 个 span、`animationName: firefly`、画面可见绿色光点 | ✅ |
| 3 | 鼠标跟随柔光 | `mousemove` 后 `--mx/--my` 更新、`.hero-glow` opacity=1 | ✅ `--mx=70.18% --my=42.98%` |
| 4 | 统计数字滚动增长 | 滚动进入后终值为 `2 / 10 / 3 / 2026` | ✅ |
| 5 | 区块交错滑入 | timeline 5 项 / feature-list 6 项 / stat-grid 4 项均带 `.reveal` 且按序延迟 | ✅ |
| 6 | `prefers-reduced-motion` 降级 | 萤火虫/柔光 `display:none`、进场动画 `none`、数字立即终值 | ✅ |
| 7 | 构建 | `npm run build` 通过 | ✅ CSS 27.63 kB / JS 163.01 kB |

截图证据：`outputs/V3-动态效果验证/`（9 张）。

> 完整设计过程见 [`交付部分/md文件/04-V3视觉重设计.md`](../../交付部分/md文件/04-V3视觉重设计.md)。

---

### 5.6 V3 扩展验收 —— 真实知识库 + 反馈闭环 + 自动化测试（2026-09-22）

对应课程「主页最终扩展（5%）」与要求中的 *basic testing* / *feedback evidence*。

**① 自动化测试（新增）**

接入 Vitest + @vue/test-utils + jsdom（仅 `devDependencies`，不进构建产物）。

```
$ npm test

 RUN  v5.0.1
 Test Files  5 passed (5)
      Tests  31 passed (31)
```

| 测试文件 | 覆盖内容 | 断言数 |
|----------|----------|--------|
| `tests/notes.spec.js` | 内容管线：加载、frontmatter 解析、日期倒序、标签去重、关键词+标签组合筛选、空结果 | 9 |
| `tests/site.spec.js` | 站点数据：项目 id 唯一且无空格、正文非空、详情图有 `alt`、外链为 https、含邮箱联系方式 | 5 |
| `tests/router.spec.js` | 路由表：五页路由存在、`/project/:id` 参数透传、路径可解析、未知地址有兜底 | 4 |
| `tests/feedback.spec.js` | 反馈出口：端点、`_captcha:false`、成功/失败/HTTP 非 2xx/非 JSON 四种判定 | 6 |
| `tests/feedback-widget.spec.js` | 反馈组件：三步状态流转、提交字段、本机去重、失败与异常不假装成功 | 7 |

**② 浏览器实测（浅色 + 深色）**

| # | 项目 | 预期 | 结果 |
|---|------|------|------|
| 1 | 知识库列表 | 显示 6 篇真实笔记 + 10 个标签，副标题「共 6 篇」 | ✅ |
| 2 | 知识库关键词搜索 | 输入 `LightGBM` 后列表由 6 条收敛为 1 条；清空后恢复 6 条 | ✅ 6 → 1 → 6 |
| 3 | 笔记深链 | 打开 `#/knowledge?note=carbon-brain-dac` 直接呈现该篇 | ✅ |
| 4 | 反馈组件 · 询问态 | 笔记/项目/首页末尾均出现「这部分内容对你有用吗？」 | ✅ 三处均验证 |
| 5 | 反馈组件 · 补充态 | 点「👍 有用」后展开文本框与「提交反馈 / 跳过」 | ✅ |
| 6 | 反馈组件 · 完成态 | 提交后显示「✅ 谢谢！你的反馈已收到（有用）」 | ✅ 用伪造 `fetch` 验证，未发送真实邮件 |
| 7 | 反馈组件 · 失败态 | 服务端返回 `success:false` 时显示原因，**不显示感谢** | ✅ |
| 8 | 联系表单回归 | 抽取公共出口后行为不变（端点/约定字段/成功判定一致） | ✅ |
| 9 | 深色主题下反馈组件 | `#/project/stock-quant` 切深色，反馈卡片样式正常 | ✅ |
| 10 | 构建 | `npm run build` 通过 | ✅ CSS 32.77 kB / JS 181.95 kB |

> 体积变化：CSS 27.63 → 32.77 kB，JS 163.01 → 181.95 kB。增量主要来自 6 篇 Markdown 正文被内联进包（内容即产品，属预期增长）。

**③ 截图证据**

`images/` 新增：`V3-知识库-搜索筛选.png`、`V3-知识库-深色.png`（已更新为真实内容版）、
`V3-反馈-1询问.png`、`V3-反馈-2补充说明.png`、`V3-反馈-3已提交.png`、
`V3-首页-反馈入口.png`、`V3-项目页深色-反馈.png`

**④ 本轮问题与修复**

| # | 问题 | 根因 | 处理 |
|---|------|------|------|
| 1 | 脚本截图连续 4 次超时且截到旧图，误以为知识库白屏 | 后台标签页不产生动画帧，`Page.captureScreenshot` 一直等不到帧 | 查 DOM 确认内容存在（`#app` 内 8745 字符）→ 经扩展 `open_url` 激活标签页后再截图 |
| 2 | 反馈组件截图停在上一轮状态（找不到「有用」按钮） | 组件状态未复位 | 截图前先 `Page.reload` 复位；演示用伪造 `fetch` 避免真实发信 |
| 3 | 站点数据测试断言失败 | 误以为 `site` 有 `role` 字段，实际为 `tagline` | 按真实字段修正测试（**测试纠正了对代码的错误记忆**） |

> 完整实现过程见 [`交付部分/md文件/06-V3扩展-知识库与反馈闭环.md`](../../交付部分/md文件/06-V3扩展-知识库与反馈闭环.md)。

---

### 5.7 项目库扩充与「关于」页改造验收（2026-09-22）

本批次把主页项目从 **2 个扩到 6 个**（含一个可双击运行的完整模拟游戏），「关于」页从 2 段扩到 6 段，知识库从 6 篇扩到 **10 篇**。

**① 自动化测试（断言数 31 → 36）**

```
$ npm test

 RUN  v5.0.1
 Test Files  5 passed (5)
      Tests  36 passed (36)
```

新增 5 条断言（`tests/site.spec.js`）：

| 断言 | 检查内容 | 为什么加它 |
|------|----------|-----------|
| 项目字段契约完整 | 每个项目必须有 `icon/short/desc/tech/role/highlights/images`，且 `highlights` 非空 | 防止新加项目时漏字段导致详情页渲染报错 |
| 二次开发类必须注明上游 | `todo-panel` 与 `claude-code-custom` 文案须同时命中 `/上游\|开源\|第三方\|MIT/` 与 `/不主张\|非原创\|不是我原创\|不是原创/` | **防止文案被无意改回成夸大表述** |
| 技能详情与首页跑马灯一一对应 | `skillDetails.length === skills.length` 且名称可匹配 | 防止展开面板与标签脱节 |
| 本地图片路径带 base 前缀 | 每个 `img.src` 以 `/` 开头且为图片后缀 | GitHub Pages 子路径下漏前缀会 404（V2 已踩过） |
| **项目的图片文件真的存在** | 用 `fs.existsSync(path.resolve('public', img.src))` 逐个核对 | 防止「改了图片格式却忘改引用路径」——本批次真的踩到了（见 ④-2） |

**①-b 反向验证（确认这条新断言不是"永远通过"）**

新加断言必须证明它挑剔得动，否则等于没写：

```
把 claude-code 的引用临时改回 .png →
  × 项目的图片文件真的存在
    AssertionError: claude-code-custom: /projects/claude-code/skills.png 找不到对应文件
还原为 .jpg → 36 passed
```

**② 浏览器实测（本地 dev 5174）**

| # | 项目 | 预期 | 结果 |
|---|------|------|------|
| 1 | 「关于」页区块完整性 | 4 个技能按钮 / 6 条项目索引 / 4 张原则卡 / 3 张「现在在做什么」/ 2 条教育经历 | ✅ |
| 2 | 技能展开（新交互） | 点击第一个技能标签后 `aria-expanded="true"`，面板展开显示对应内容 | ✅ |
| 3 | 无字面 Markdown 残留 | 页面上不出现裸露的 `**` | ✅ 全部区块检查通过 |
| 4 | 项目详情页渲染 | `#/project/weiguan-yifang` 标题正确、正文标题层级正确、无字面 `**` | ✅ 正文 632 字符 |
| 5 | 新素材加载 | 新引入的项目图能正常加载 | ✅ `naturalWidth = 1500` |
| 6 | 知识库篇数 | 侧边栏显示「共 10 篇」，新增 4 篇可打开 | ✅ |
| 7 | 构建 | `npm run build` 通过 | ✅ CSS 37.52 kB / JS 218.83 kB |

**③ 内容质量核查（本批次新增的一道检查）**

新增了一条**文案正确性检查**：把 Markdown 逐段解析成 HTML，扫描是否残留未被解析的 `**`。原因是在写《回测数字的陷阱》时发现 —— **`**` 后若紧跟中文标点，CommonMark 不会当作加粗闭合**，页面上就会露出裸露的 `**`。检查后修掉了 1 处。

| 检查 | 范围 | 结果 |
|------|------|------|
| Markdown 加粗解析 | 10 篇笔记 | ✅ 修正 1 处后全部通过 |
| Markdown 加粗解析 | 6 段项目正文 | ✅ 全部通过（用正则抽取模板字符串后单独解析，不依赖打包环境） |
| 图片体积 | `public/projects/` | ✅ 单张 4.5 MB → 压缩后总计 3.0 MB |

**④ 本轮问题与修复**

| # | 问题 | 根因 | 处理 |
|---|------|------|------|
| 1 | 构建直接失败（Parse failure） | 项目文案里用反引号写了 `app.py`，破坏模板字符串 | 改成普通文字表述 |
| 2 | 图片转换后引用失效 | 批量把 `.png` 转成 `.jpg` 并删除原文件，但代码里仍引用 `.png` | 把代码里的引用路径一并改掉（**改文件和改引用必须成对做**） |
| 3 | 想用 Node 直接 import `site.js` 检查文案失败 | 纯 Node 环境没有 `import.meta.env`（`asset()` 依赖它） | 改成用正则抽取模板字符串后单独解析 |
| 4 | 「关于」页技能标签"看着能点但点不动" | 原来是 `<span>` + hover 动画，没有行为 | 改成 `<button>` + `aria-expanded` 展开面板 |
| 5 | 用 CDP 检查「关于」页时命中了旧标签页 | 调试脚本按 `.find()` 取**第一个**匹配标签，多标签时命中的是旧页面 | 改为取**最后一个**匹配标签（`.filter().pop()`） |
| 6 | 截图里页面内容为空（游戏试玩页） | 未聚焦/后台标签页不产生动画帧，CSS 入场动画不推进，元素停留在 `opacity:0` | 截图前注入脚本强制 `animation/transition/opacity` 复位后再截 |

**⑤ 截图证据**

`images/` 新增：`V3-关于页-技能展开.png`、`V3-首页-项目列表.png`、`V3-项目页-为官一方.png`

> 完整实现过程见 [`交付部分/md文件/07-项目库扩充与关于页改造.md`](../../交付部分/md文件/07-项目库扩充与关于页改造.md)。

### 5.8 上线与线上复验（2026-09-22）

**发布记录**

| 分支 | 提交 | 说明 |
|------|------|------|
| `gh-pages` | `0654db3` → `fd3569e` → `c17f89c` | 站点产物；`c17f89c` 为含水印修复的最终版 |
| `main` | `7b0079d` → `e39d118` | 源码；以 `-s ours` 合并了远端原有的 V1/V2/V3 历史，**保留远端提交记录、推送为快进而非强推** |

> 推送过程中网络多次抖动（`github.com` 间歇不可达、`Failed to connect ... after 75004 ms`、`Empty reply from server`），最终以 HTTP/1.1 + 降低 `lowSpeedLimit` 的重试方式推送成功。

**线上复验（公网真实页面，CDP 实测）**

| # | 检查项 | 结果 |
|---|--------|------|
| 1 | 线上 bundle 已更新 | ✅ 页面引用 `assets/index-ELjfodgb.js` / `index-CrfiSzA_.css` |
| 2 | 首页统计显示「6 项目实践」 | ✅ |
| 3 | 首页项目入口 | ✅ 6 个：`weiguan-yifang` / `carbon-brain` / `stock-quant` / `todo-panel` / `claude-code-custom` / `ccswitch` |
| 4 | 首页图片（含经历区旧图） | ✅ 14 张全部加载，`naturalWidth` 分别为 1280/824/1500/1706/1280/1500/1500/1200/1200/900/900/750/1200/900 |
| 5 | 「关于」页技能展开 | ✅ 4 个 `button[aria-expanded]`，点击后 `false → true`，面板显示真实内容（如「Python / 主力语言 / 从数据处理到模型训练……」） |
| 6 | 「关于」页区块 | ✅ 「现在在做什么」「专业技能」「项目」「工作方式」「教育」全部存在 |
| 7 | 知识库篇数 | ✅ 显示「共 10 篇」，标签新增「工程 / 合规 / 游戏 / 复盘」 |
| 8 | 6 个项目详情页路由 | ✅ 均可打开，正文与「上游/开源/二次开发/第三方」声明描述符符合预期 |
| 9 | 全站图片 HTTP 探测 | ✅ `public/` 下 22 张图全部 `200`（其中 1 张首次探测 `000`，复测 `200`，判定为网络抖动而非 404） |

> 说明：CDP 在页面内读 `img.naturalWidth` 时，懒加载与并行加载会让少数图短暂读到 `0`；因此**判定"是否断图"以 HTTP 状态码与 `naturalWidth` 复核为准**，不能只看一次快照。

> ⚠️ **后记（2026-09-22）**：上表第 2、3、8 行记录的是**当时的**验收结果（6 个项目）。其中「额度切换小工具」后来因授权边界被主动撤下展示，项目数变为 5、统计同步改为「5 项目实践」。本节**不改写历史**，改动与理由见 §5.10。

**④-b 素材水印问题（本批次发现的合规瑕疵）**

项目背景图 `public/projects/weiguan-yifang/bg.jpg` 是用文生图工具生成的，图面右下角带服务商水印「DeepWorks AI」。这是**第三方标识出现在自己的作品里**，无论是版权观感还是作品完成度都不该留。

处理过程：

| 步骤 | 做法 | 结果 |
|------|------|------|
| 1. 定位 | 用 PIL 逐像素扫描，测出水印在 1500×2250 原图中的范围：`x 1097–1446`、`y 2146–2201` | ✅ 精确到像素 |
| 2. 试探裁剪 | 先裁到 1500×1980 | ⚠️ 发现 `y2146–2250` 一带存在深色墨迹（小船），裁掉会切到画面主体，**放弃裁剪** |
| 3. 改为修补 | 取水印矩形上下各 3 行干净像素，逐列线性插值填补，再叠加 ±2 的轻微噪点打破条带感 | ✅ 尺寸保持 1500×2250 不变，画面完整 |
| 4. 验收 | 水印区亮白像素 0；边界剖面无台阶；修复区灰度标准差 5.03，落在周边干净水面自然区间 1.53–8.63 内 | ✅ 肉眼无可见色块 |
| 5. 线上确认 | 线上 `bg.jpg` 与本地 `sha256` 完全一致（`8a593921…8fed`，507,617 字节） | ✅ 线上已生效 |

---

### 5.9 成品感补强：逐页标题 · 分享信息 · 键盘可达 · 404 页 · 图片防跳动（2026-09-22）

对应《优化方案》§2.3 工程级缺口 1/2/3/4/5/7（批次 B1–B5）。这一批不做新功能，全部围绕一件事：**别人点进来，第一眼能不能看出这是个正经成品。**

**① 改了什么**

| 编号 | 改动 | 落地位置 |
|------|------|----------|
| B1 | 每个页面独立标题：「关于我 · 刘博康」「TO-DO Panel 桌面面板定制 · 刘博康」，笔记页精确到笔记名 | `src/data/meta.js` + 路由 `meta.title` + `src/App.vue` |
| B2 | 分享信息与搜索引擎文件：`description` / `canonical` / `og:` / `twitter:` 逐页同步；新增 `robots.txt`、`sitemap.xml` | `src/data/meta.js`、`index.html`、`public/robots.txt`、`public/sitemap.xml` |
| B3 | 键盘可用：全局 `:focus-visible`、页首「跳到主要内容」、当前栏目 `aria-current`、导航按钮 `aria-expanded`/`aria-controls`、灯箱 Esc 关闭并把焦点还回图片按钮 | `src/style.css`、`src/App.vue`、`src/components/SiteHeader.vue`、`src/views/ProjectDetailView.vue` |
| B4 | 真 404：未知地址渲染「这个页面不存在」并回显错误路径（不再静默跳回首页）；另有 `public/404.html` 兜住"把网址打错"的情况 | `src/router/index.js`、`src/views/NotFoundView.vue`、`public/404.html` |
| B5 | 图片防跳动：构建期扫描 `public/` 真实像素尺寸生成尺寸表，全站 `<img>` 补 `width`/`height`，CSS 补 `img{max-width:100%;height:auto}` | `scripts/gen-image-sizes.mjs` → `src/data/image-sizes.js`、`src/style.css` |

**② 自动化测试（断言数 36 → 59）**

```
$ npm test

 Test Files  6 passed (6)
      Tests  59 passed (59)
```

| 变化 | 内容 |
|------|------|
| 新增 `tests/a11y-seo.spec.js`（19 条） | 逐页标题、`description`、`canonical`、`og:`；「跳到主内容」链接存在且指向 `#main`；未知地址必须落到兜底组件而不是重定向首页 |
| `tests/site.spec.js` +3 条 | 每张图都能查到真实宽高、尺寸表里的数值都是正数、带 base 前缀与不带前缀查到同一张图 |
| `tests/router.spec.js` +1 条（并改 1 条） | 末条由「未知地址重定向首页」改为「未知地址落到兜底组件」；新增「地址里的 `#` 不影响匹配」 |
| `package.json` | 新增 `npm run gen:images`，尺寸表可随时重新生成 |

**③ 反向验证 —— 这批最有价值的一条经验**

新加的断言必须"挑剔得动"。做法：**故意把被测对象改坏，看断言会不会变红**。

| 轮次 | 故意弄坏什么 | 结果 |
|------|--------------|------|
| 1 | 删掉 `img { height: auto }` | ✅ 变红 |
| 2 | 把兜底路由改回"重定向首页" | ❌ **没变红** |
| 3 | 把图片指向不存在的文件 | ✅ 变红 |

第 2 轮是关键发现：**原断言只检查"路由名是不是 `not-found`"，而一条 `redirect: '/'` 的路由名字也可以叫 `not-found`** —— 页面其实跳走了，测试却给绿灯。

于是把这 1 条断言改成同时检查三件事：只匹配到 1 条记录、该记录**挂着真实组件**、且**没有 `redirect`**。改完再弄坏一次 → 变红；还原 → 全绿。

> **"测试通过"不等于"验证到位"。测试也会骗人，除非你亲自试着让它失败一次。**
> 顺带说明反向验证的收尾纪律：弄坏的文件一律先用备份还原、跑一遍全绿、确认 `git status` 只剩预期改动，再删备份。

**④ 浏览器实测（dev 与线上各一轮，用真实按键与真实点击）**

| # | 检查项 | 结果 |
|---|--------|------|
| 1 | 逐页标题 | ✅ 线上 7 条路由标题全对（首页 / 关于我 / 两个项目名 / 笔记名 / 联系我 / 页面不存在） |
| 2 | 每页只有一个 `<h1>`，且与页面主题一致 | ✅ 依次为「刘博康Curious Builder」「刘博康」「《为官一方》古风县令治理模拟」「TO-DO Panel 桌面面板定制」「知识库 · 笔记」「联系我」「这个页面不存在」 |
| 3 | 键盘：Tab 到「跳到主要内容」 | ✅ 可聚焦、文字正确、按下后地址栏不变（仍 `#/`）、焦点落到主内容区 |
| 4 | 键盘：Enter / Esc 操作大图 | ✅ Enter 打开灯箱并聚焦关闭按钮；Esc 关闭并把焦点还给原来那张图 |
| 5 | 导航当前项高亮 | ✅ 关于页只有「关于」带 `aria-current`；知识库页只有「知识库」 |
| 6 | 图片尺寸与断图 | ✅ 线上首页 14/14、项目页 3/3 与 2/2 均带 `width`/`height`，断图 0 |
| 7 | 线上静态文件 | ✅ `robots.txt` 200（245 B）、`sitemap.xml` 200（551 B）、`404.html` 200（1764 B） |
| 8 | 线上输错网址 | ✅ 返回 **HTTP 404**，且内容是我们自己的「页面不存在」页面 |
| 9 | 线上资源完整性 | ✅ 新 JS/CSS 均 200；`avatar.jpg`、`weiguan-yifang/bg.jpg`、`art/hero-forest.jpg`、`stock/backtest.png` 均 200 |

**⑤ 过程中的一次"假警报"（值得记下来）**

线上复验时一度出现**内容不换、始终停在首页**：标题跟着路由变，但 `<h1>` 与正文一直是首页的。看着像严重的线上回归。

查证过程：

| 步骤 | 观察到的现象 |
|------|--------------|
| 1 | 直接用调试协议问页面：`requestAnimationFrame` 在 1 秒内触发了 **0 次** |
| 2 | 同一时刻 `document.visibilityState` 是 **`hidden`**（标签页在后台） |
| 3 | 路由容器的 class 卡在 `page-leave-active`，没进到下一步 |

结论：**页面切换动画依赖浏览器的动画帧**。标签页在后台时浏览器不产生帧，动画停在半路，新页面自然挂不上来。这是**测试环境的限制，不是线上缺陷** —— 真实用户看着窗口时有帧，0.25 秒就切完（本地 dev 实测最长 1.3–2.0 秒内也会自行完成）。
把"帧"临时换成定时器后再跑，线上 7 条路由全部正常，据此确认线上无恙。

> 教训：**验证页面状态既不能"打开就开始读"，也不能用固定等待**。要么轮询到状态真正落定，要么先确认当前标签页确实在前台（`visibilityState === 'visible'`）。

**⑥ 发布记录**

| 分支 | 提交 | 说明 |
|------|------|------|
| `gh-pages` | `c17f89c` → `b48816d` | 站点产物。变更为：新增 `404.html` / `robots.txt` / `sitemap.xml` 与新的 JS/CSS，删除两个旧资源，`index.html` 更新；**共 8 个文件，无意外增删** |
| `main` | `72e8ba4` → `63b44fe` | 源码；仍为快进推送，未强推 |

> 两个细节：① 本次**没有**删除 `gh-pages` 上的 `.nojekyll`（GitHub Pages 需要它）；② 水印修复版 `bg.jpg` 上线前后 `sha256` 与字节数完全一致（`8a593921…8fed`，507,617 字节）。

### 5.10 撤下一个展示项目、内容并入相邻项目（2026-09-22）

**① 决定与理由**

作品集里原本有一个自研小工具（本机 Node 中转服务 + macOS 菜单栏开关，用来在多套自有 API 配置之间切换）。复查时认为：这件事涉及**改动上游产品既定的使用方式**，而我**没有取得上游官方的认可**，把它当成作品公开陈列并不合适。

处理方式不是简单删掉，而是三件事：

| 做法 | 说明 |
|------|------|
| 不再单独展示 | 首页卡片、项目详情页、首页统计数字一并去掉 |
| 有价值的部分并过去 | 本机 Node 中转服务 + Swift 菜单栏开关 + 启动脚本，并入「第三方桌面应用本地二次开发」，成为其中一个章节 |
| 写清归属 | 并入时把**上游应用的部分**与**我自己写的小工具**分开写，并说明这部分自研 |
| 只讲技术、不作推荐 | 措辞只说「在多套**自有** API 配置之间切换」这个实现，不写成对某个产品的使用建议 |

**② 改了哪些文件**

| 文件 | 改动 |
|------|------|
| `src/data/site.js` | 删除该项目的完整条目；技术内容作为新章节并入「第三方桌面应用本地二次开发」；该项 `tech` 增加 `Node.js / Swift`、`highlights` 增加一条 |
| `src/views/HomeView.vue` | 统计数字 `6 项目实践` → `5 项目实践` |
| `tests/site.spec.js` | 新增「已决定不展示的项目不能出现在展示列表里」断言（防止以后被误加回来） |
| `src/views/ProjectDetailView.vue` | 顺带修复：项目 id 不存在时会渲染一个**没有标题、也没有 `<h1>`** 的空壳页；改为复用全站 404 页面，并把标签页标题改成「页面不存在」 |

**③ 自动化测试（断言数 59 → 61）**

- `npm test`：6 个 spec / **61 条断言全部通过**。
- 新增的 2 条断言都做了**反向验证**（故意改坏 → 应当变红）：
  - 故意把该项目加回 `projects` → 「不能出现在展示列表里」**变红** ✅（`expected { id: 'cc…' … } to be falsy`）
  - 故意把项目页兜底改回空壳页 → 「进同一个 404 页」**变红** ✅（`expected '项目不存在。' to contain '这个页面不存在'`）
  - 两处还原后重新全绿。

**④ 线上复验（公网真实页面，CDP 实测）**

| # | 检查项 | 结果 |
|---|--------|------|
| 1 | 线上 bundle 已更新 | ✅ 页面引用 `assets/index-2WWTbsIn.js` / `index-56smf-Gt.css` |
| 2 | 首页项目入口 | ✅ 5 个：`weiguan-yifang` / `carbon-brain` / `stock-quant` / `todo-panel` / `claude-code-custom`（**已无**被撤下的那个） |
| 3 | 首页统计 | ✅ `5 项目实践` / `10 架子鼓等级` / `3 实践经历` / `2026 入学天大` |
| 4 | 「关于」页项目索引 | ✅ 同步为 5 个 |
| 5 | 被撤下项目的旧地址 | ✅ 进入「这个页面不存在」页，标签页标题为「页面不存在 · 刘博康」 |
| 6 | 保留的项目页 | ✅ 「第三方桌面应用本地二次开发」标题、`<h1>` 与正文（含新并入的章节）正常 |

> 排查记录：第一次复验时统计数字读到的全是 `0`，一度以为改坏了。逐个排除后确认是**假警报**——统计是滚动进视野后才开始递增，而这个自动化浏览器里 `scrollIntoView()` 不改变 `window.scrollY`，页面其实没滚过去；真正滚到位后数字是 `5 / 10 / 3 / 2026`。**又一次印证：读页面状态前，要先确认页面真的处于你以为的状态。**

**⑤ 发布记录**

| 分支 | 提交 | 说明 |
|------|------|------|
| `gh-pages` | `b48816d` → `0cd3c4f` → `f473cc1` | 站点产物。第一次只动新 JS/CSS 与 `index.html`（**无意外增删**）；第二次加上项目页 404 兜底修正 |
| `main` | `63b44fe` → 见下 | 源码；快进推送，未强推 |

**⑥ 补充说明：这不是"删掉不好看的东西"**

撤下的理由是**授权边界**，不是效果不好——这个小工具我每天都在用，代码也是我自己写的。问题在于：**把"改动上游产品既定用法"的做法当作品公开陈列，会被理解成一种推荐**，而这超出了我获得授权的范围。所以处理原则是：留下能证明技术能力的部分（本机服务 + 原生小工具 + 脚本），收起源头用法相关的部分。

---

### 5.11 响应式 A 档：手机横向溢出修复 + 点击区域达标 + 移动菜单三项（2026-09-25）

**① 起因与方法**

用 CDP 在 9 种设备尺寸 × 5 条路由上做了全量响应式体检（量测脚本 `.deepworks/cdp-tool/resp_audit.mjs`、窄屏探针 `resp_probe320.mjs`），确认唯一的真缺陷是「关于」页在手机宽度的**横向溢出**。随后按《响应式适配方案（手机/平板/电脑）》的 A 档实施，改动只涉及 3 个文件的样式与头部交互：`AboutView.vue`、`style.css`、`SiteHeader.vue`。

**② 根因（三行 CSS 的相互作用）**

工作项是「图标 | 文字块 | 角色标签」横排：文字块 `flex: 1; min-width: 0`（可以被压到 0），角色标签 `flex-shrink: 0`（不许压缩，最长一条实测 366px）。两者相加超过手机宽度 → 内容最小宽度锁在 **459px**（320~414 五档实测一致），标题被挤成 32×331 的竖排。已有的 `flex-wrap: wrap` 救不了——文字块初始宽度为 0，"永远装得下"，浏览器没有换行理由。

**③ 修法与实测结果（同一套脚本改前/改后对比）**

| 项 | 改前 | 改后 | 方法 |
|---|---|---|---|
| 「关于」页横向溢出 @375 | **84px** | **0** | 媒体查询改成 CSS 网格：第一行「图标+标题/说明」，角色标签独占第二行 |
| 「关于」页横向溢出 @390 | **69px** | **0** | 同上 |
| 工作项标题 @375/390 | 32×331（竖排） | 224×31（正常） | 同上；`work-body` 从 0~24px 恢复到 188–282px |
| 320 / 360 / 414 溢出 | 缩放塞下/溢出 | **全部 0** | 同上 |
| 汉堡键 | 23×31 | **44×44** | `min-width/min-height`，`justify-content: flex-end` 保持 ☰ 原位 |
| 主题切换（手机） | 36×36 | **44×44** | 仅 ≤768 媒体查询内 |
| 菜单链接（手机） | 30×21 | **30×44** | `min-height: 44px`；下划线锚点改为 `calc(50% - 16px)` 跟随文字 |
| 返回链接 / 筛选片 / 反馈按钮（手机） | 28 / 32 / 43 高 | **44 高** | padding 或 `min-height`，仅 ≤768 |
| 工作项标题链接 | 23 高 | 31 高 | 行内元素纵向 padding：只大可点区域，不动排版 |
| 手机"触控 <44px"数量 | 首页 13 / 知识库 22 / 关于 11 | **6 / 2 / 6**，且剩余全部 ≥24×24（WCAG 2.2 AA 达标） | 见上 |
| 收起菜单可被 Tab 选中 | 能（焦点跳进看不见的菜单） | **不能** | `visibility: hidden` 写进窄屏媒体查询（没用 `inert`：`inert` 绑状态，横屏时会误锁桌面导航；`visibility` 绑宽度，规则自动失效） |
| 打开菜单背景滚动 | 跟着滚 | **锁住**（`body.menu-open { overflow: hidden }`，仅在窄屏媒体查询内，横屏自动解锁） | `SiteHeader.vue` 监听 `menuOpen` |
| 菜单位置 | 写死 74px（与实际 75px 差 1） | `--header-h` 变量两处共用，实测菜单顶边正好 74 | `style.css` |

**④ 桌面回归（不能因为修手机把电脑改坏）**

@1440：导航链接 30×21 不变 ✓、主题键 36×36 不变 ✓、链接下划线 `bottom: -6px` 不变 ✓、桌面导航 visibility 正常 ✓、文档无横向溢出 ✓。

**⑤ `html { overflow-x: clip }` 的安全性验证（新增的全局保险）**

- 对照实验：带着这条规则 `window.scrollTo` 能滚（scrollY 484，平滑滚动中途读数），摘掉后能滚到 700 —— **滚动不受 clip 影响**。
- 滚动到 scrollY=800 时吸顶头 `top=0`、`position: sticky` —— **吸顶正常**。
- 选 `clip` 不选 `hidden` 的原因：`hidden` 会建立滚动容器、可能破坏 sticky；`clip` 不会。

**⑥ 自动化测试**：`npm test` 6 个 spec / **61 条断言全部通过**（本次为样式与交互改动，未新增断言）。

**⑦ 排查记录（本次踩的环境坑，供以后参考）**

1. 自动化浏览器里**后台标签页 `visibilityState` 恒为 `hidden`，CSS 过渡时间线冻结**：菜单打开后读到的 `visibility`/`transform` 停在变化前的值，一度误判"规则没生效"。解法：注入 `transition: none` 后纯验规则匹配（开→`visible`、关→`hidden` 全对），动画行为留给真机。
2. 同一环境里 `window.scrollTo` 在部分标签页完全无效，需换到"活"的标签页，并先读 `scrollY` 做对照，避免把环境问题当成代码回归。
3. `Page.captureScreenshot` 在本环境会挂死（看门狗 90s 强杀）——视觉证据一律以量测数字为准。

**⑧ 待办**：部署到线上后做**真机自测**（手机上打开线上地址：横向不滑、☰ 好点、菜单开时背景不滚、字看得清、底部按钮不被 Home 条挡、键盘 Tab 不进收起菜单）。

---

## 六、测试结论

- **功能完整性**：V2 九大功能全部通过自测；V3 在保留全部功能的基础上通过双主题视觉验收。 ✅
- **自动化测试**：知识库内容管线、站点数据、路由表（含 404 兜底与 hash）、反馈出口、反馈组件、项目字段契约与内容口径、逐页标题与分享信息、跳转链接与图片尺寸覆盖，共 **6 个 spec / 61 条断言全部通过**，其中新增断言均通过"故意改坏看它会不会变红"的反向验证。 ✅
- **内容准确性**：新增的工作区项目均来自真实产出（有产出才写，空目录工作区不写）；涉及数字的表述已补全口径；二次开发项目已标注上游来源。其中 1 项后来因授权边界主动撤下展示并并入相邻项目（见 5.10）。 ✅
- **展示口径自查**：对"未取得上游官方认可"的用法类做法，主动撤下其展示、只保留自研技术部分并写明归属；并加了一条自动化断言防止它被误加回列表。 ✅
- **素材合规**：AI 生成素材上残留的服务商水印已定位并修复（未裁剪掉画面内容），线上文件与本地 `sha256` 一致。 ✅
- **线上可用性**：公网首页与全路由渲染正常，资源加载无误；V3 原创素材与 bundle 均 200。 ✅
- **关键业务流程**：联系表单在线上真实端到端可用；内容反馈组件已在首页、项目页、笔记页三处可用。 ✅
- **满足评审标准**：项目**既可本地运行，也可公网访问**（runnable AND publicly accessible），且带有**基础测试**与**反馈机制**。 ✅

---

## 七、后续测试计划

- [ ] 补充移动端真机测试（不同机型/浏览器）。
- [ ] **收集真实用户反馈（不少于 5 条）**并填入《用户反馈与改进记录》，形成优先级修订清单。—— 课程禁止编造，须由本人实际收集
- [ ] 对知识库、项目详情等内容更新后做回归测试（`npm test` + 页面复验）。
- [x] ~~按权威标准体检补齐：每页独立标题（WCAG 2.4.2）、键盘焦点与「跳到主内容」（WCAG 2.4.7 / 2.4.1）、404 页、`<img>` 补 `width/height` 治理 CLS~~ —— 已于 2026-09-22 完成并上线，见 5.9
- [ ] 跟随系统深浅色（`prefers-color-scheme`，批次 C1）。
- [ ] 重写 `README.md`（现仍为脚手架原文）。
- [x] ~~线上复验本批次变更（6 个项目页、「关于」页技能展开、10 篇笔记）~~ —— 已于 2026-09-22 完成，见 5.8
- [x] ~~撤下一个未获上游认可的项目展示、技术内容并入相邻项目，并线上复验~~ —— 已于 2026-09-22 完成并上线，见 5.10
- [ ] 数字人对话：先做路线决策（关键约束：**API Key 不能放在前端**），见 `docs/数字人对话-准备清单.md`。
- [ ] 若后续继续用文生图素材，**交付前必须逐张检查右下角水印**（本批次踩过一次）。


---

_本记录基于真实测试过程整理，作为过程证据归档。_

---

## 5.12 手机端反馈四修（2026-09-26，main `2a24e9d` / gh-pages `3eeb37f`）

用户真机试用 A 档后反馈 4 个问题，本批全部处理并部署。

### 问题与修法

| # | 用户反馈 | 根因 | 修法 |
|---|---|---|---|
| 1 | 项目顺序要调整 | 原按「可演示程度」排序 | 改为：Carbon Brain → 股票量化 → Claude Code 二开 → 为官一方 → TO-DO Panel（site.js） |
| 2 | 「回到顶部」按钮按了无反应 | **Vue 模板表达式访问不到 `window` 全局对象**（白名单不含 window），`@click="window.scrollTo(...)"` 点击必报 TypeError——该按钮自写下之日起从未生效，所有设备都是 | 挪进 `<script setup>` 的 `toTop()` 方法，加 iOS 旧版 scrollTo 参数兜底（SiteFooter.vue） |
| 3 | 网卡多点两下页面被双击放大；页面卡 | viewport 未限制双击缩放；吸顶头 `backdrop-filter: blur(16px)` 让下方滚动内容每帧重绘 + 22 个萤火虫无限动画 | `html{touch-action: pan-x pan-y pinch-zoom}`（禁双击放大、**保留捏合缩放**、附消除 300ms 点击延迟）；≤768px 头部改 96% 实色去模糊、萤火虫整体隐藏（桌面不变） |
| 4 | 项目展示图只露角落 | 首页画框 `aspect-ratio:16/11 + object-fit:cover`，而 stock 回测图 1280×**2200**、为官一方事件图 1500×**3136** 等超长图被放大约 2.6 倍，只显示顶部一截；详情页画廊 `height:240px + cover` 同理 | ① 为 5 个项目重制 **1600×1000 (16:10) 门面卡**：横图裁关键区（stock 取指标卡+净值曲线）、竖屏游戏图取政令卡阵横带；素材从各项目工作区新挖（TO-DO Panel 换 `docs/screenshots/` 3442×2216 高清原图、为官一方换 `recap_frames/` 真实游戏截图）② 详情页画廊 cover→contain 完整显示 |

### 验证

- `npm test` 61/61 ✓（新增门面卡尺寸已入 image-sizes 注册表，防 CLS 断言通过）
- dev CDP（390px）：顺序 ✓、5 门面卡加载 ✓、回顶按钮 scrollTo 调用参数正确 ✓（逻辑通）、touch-action=manipulation ✓、头部无模糊 ✓、萤火虫隐藏 ✓、横向溢出 0 ✓
- 线上 CDP（390px，真实公网页面）：顺序 ✓、5 门面卡 1600×1000 全部加载 ✓、横向溢出 0 ✓、头部 backdrop-filter=none ✓

### 环境备注

- 后台标签页渲染冻结导致 smooth 滚动行为异常（scrollY 反涨），回顶最终效果以真机为准（本地已确认事件与方法链路打通）。
- 线上 lazy 图片需滚入视口 + 等 load 后再断言，500ms 间隔会误判加载失败。

### 未来计划（本批未做，用户同意后续再议）

- 「关于」页经历画廊（`.exp-img` 4/3 cover）同样存在裁剪，人物照影响小，暂未改。
- Carbon Brain 无软件界面素材（工作区只有发票扫描件），门面卡暂用团队合影；如需界面图须重新跑程序截图。
- 手机端深度性能（Lighthouse 实测、图片压缩 webp 化、 marquee 动画降级）未做系统测量。
- 真机自测清单待用户本轮试用后回填。
